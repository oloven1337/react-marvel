const initialState = {
    isLoaded: false
}

export const comicsReducers = (state = initialState, action) => {
    switch (action.type) {

        default: {
            return state
        }
    }
}
